engines:

    test_engine:

        location: {'type': 'dev', 'path': '{CONFIG_FOLDER}/bundles/test_engine'}

        apps:
            test_app:
                location: {'type': 'dev', 'path': '{CONFIG_FOLDER}/bundles/test_app'}

frameworks:
  tk-framework-desktopserver_v1.x.x:
    location: {type: path, path: '$FRAMEWORK_ROOT' }
